/**
 * Empty browser stub for Node.js built-in modules that are imported
 * transitively but never actually called in browser code paths.
 * Aliasing to this stub keeps esbuild from generating require() calls
 * that would throw at runtime in the browser.
 */
export default {};
